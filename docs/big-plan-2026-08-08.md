# rick (G:\projectE) — Master Audit & Plan

Date: 2026-08-08 · Scope: last session reconstruction, resource audit (RAM/CPU/disk),
bug review, optimization backlog, prioritized work plan.

## 1. Last session reconstruction (2026-08-08 17:57–19:39)

Session `2026-08-08T17-57-39_be64.json` ("Investigate rick at G:\projectE why cpu usage
spikes"), 286 requests, 91.2% cache hit, 193 bash tool calls, 10 tool errors.

What it found and fixed (all verified present in the working tree):

| # | Fix | Location | Why it mattered |
|---|---|---|---|
| 1 | `trimTranscript` off-by-one: trimmed to 501 instead of ≤500 | `internal/tui/model.go:1137` | Every frame (25 Hz drain + 90 ms spinner) re-entered the trim branch, invalidated the whole render cache and re-rendered ~500 blocks: **953 µs/chunk vs ~50 µs (18×)**, 64% of CPU in `renderMsg` |
| 2 | `maskJSON` sliced `value[:157]` mid-rune → `\ufffd` in model view | `pkg/contextbudget/contextbudget.go:607` (`cutRunes`) | Corruption of provider-facing bytes |
| 3 | capLive marker lacked the retrieval key | `pkg/contextbudget/contextbudget.go` | Model couldn't round-trip the payload (`retrieve_uncompressed_context` requires a key) |
| 4 | **Warm/stream byte mismatch** (root cause of 3 full re-bills = 650,867 of 857,624 input tokens, 75.9%) | `internal/provider/openai/openai.go:598` (`buildWireBody`) | `Warm` built its own body, omitting the DeepSeek reasoning dialect and keeping temperature → primed a different prefix than `Stream` sent → every turn boundary re-billed ~270–370k tokens cold |
| 5 | Deployed rebuilt `rick.exe` to 4 launch points | `G:\projectE\rick.exe`, `G:\projectE\rick`, `~/bin/rick`, `~/bin/rick.exe` | All md5 `2a1b70161c…` (61,772,288 B), verified byte-identical to a fresh `go build` today |

Session tail also contains the earlier cache-hardening work (P1–P5, reasonix plan items 1–5,
CPU plan §5): boot byte-stability test, canonical sorted tool JSON, fail-closed divergence
(`inferReason`), `cmd/cachehit` driving the real Runner loop, `/stats` resets counter,
P1c full-prefix re-warm, earlier distill (70%→55%, `LiveZoneTurns` 2→1, `DistillRatio` 0.5→0.4).

Benchmark standing (real Runner loop, 50 turns × 2): **92.63% / 89.68% TOTAL hit, 0 resets,
0 unexpected divergences over 100 turns** — every remaining re-bill is provider-side
(free `-flash-free` tier LRU/TTL eviction), not a client byte change.

## 2. Resource audit

### System state (at audit time)
- RAM: 31.6 GB total, 8.2 GB free · CPU: Ryzen 5 7500F, 6C/12T, 8% idle
- No rick process was running; footprint measured statically + from profiles

### Disk
| Item | Size | Verdict |
|---|---|---|
| `~/AppData/Local/rick/snapshots` | **64 GB** (of which `C_Users_einme_Downloads` = 64 GB) | **#1 issue** — one snapshot tree of Downloads dominates; only 8 of 11 snapshots have content |
| Stale `.exe`s in repo root | **328 MB** (11 files: cachehit 34 MB, tui.test 53 MB, agent.test 32 MB, rick-delay-* 24 MB ea, hermes-verify 24 MB, vision 11 MB, ricksec 5.8 MB, rickverify 25 MB) | Cleanup candidates; `rick.before-*` backups follow a convention but multiply |
| `~/AppData/Local/rick/sessions` | 35 MB, 415 files | Grows with usage; fine today, needs archiving policy |
| `~/AppData/Local/rick/goals` | 22 KB | Fine |
| Project `G:\projectE` | 853 MB | Mostly git + build artifacts |

### RAM/CPU (code-level, from last session's profiles + this review)
- Fixed this session: per-frame full-transcript re-render (see §1).
- Remaining per-turn cost: `buildRequest` BPE-counts the whole history several times per
  turn (`countMessages`/`countJSONValues` + `history.Retain`/`retainStable` re-tokenize
  after `json.Marshal`) — S1. `ChooseBoundaries` marshals every message twice (hash +
  byteLen) + sha256 per message, every turn and again after distill — S2/S5.
- `usage.json` full marshal+rename every 30 s while turns are active (S4, debounced, fine
  unless file grows).
- `websearch.go` already hoisted `http.Client`/regexes to package level (S6 ✅, was pending
  in CPU plan §3).

## 3. Bug review findings

1. **Everything is uncommitted.** 33 modified files (+2,151/−307) + 18 untracked (all five
   fixes, all new tests, `cmd/cachehit`, `scripts/check-cache-guard.sh`,
   `.github/workflows/cache-impact.yml`, plan docs). The deployed binary matches the
   working tree, but `git checkout .` would silently lose every fix. **Highest-risk item.**
2. **Warm/stream retention asymmetry (latent).** `Warm` (openai.go:562) forces
   `PromptCacheRetention = "24h"` unconditionally; `buildWireBody` only sets it for
   `CacheRetentionLong` (line 628). Hashed bytes match so it is harmless today, but a
   future "send retention only when the stream would" refactor must keep the two in sync —
   codify it in `warm_parity_test.go` (currently only asserts DeepSeek dialect).
3. **P1b/P1c latch reviewed**: `cacheEvicted` latches one-shot and resets after the warm
   dispatch (agent.go:430); `warmNeeded` also covers head-trim, turn-0 resume, and
   idle-gap > TTL. Logic is sound.
4. **`analyze_sessions.py` reset heuristic** labels pre-v0.1.13 sessions with `?` causes —
   expected (no persisted divergence rows); don't read those as bugs.
5. **Provider-side evictions are the residual miss cause** (proven: 0 client divergences in
   the 100-turn benchmark). Any further 92%→98% work must distinguish provider tier from
   client bytes — the harness now does.
6. **`maxDistillToolArgChars` folding** (`summarizeToolArgs`) is deterministic (sorted keys)
   — good; only fires >800 B, so subagent delegation prompts no longer re-broadcast.

## 4. The plan

Prioritized by risk × effort × impact.

### A. Secure the work (do first, today)
- [ ] A1. Commit the WIP as one Conventional Commit batch (e.g. `feat(cache): warm/stream
      parity, runnable harness, byte-stable boot`) with the cache-impact guard check.
- [ ] A2. Confirm `.github/workflows/cache-impact.yml` runs on PRs and add `Cache-impact:`
      labels to the commit.
- [ ] A3. Delete stale scratch binaries/backups from repo root that are not part of the
      deliverable: `agent.test.exe`, `tui.test.exe`, `cachehit.exe` (keep `cmd/cachehit`
      source), `rick-delay-*.exe`, `hermes-verify.exe`, `vision.exe`, `ricksec.exe`,
      `rickverify.exe`, `rick.before-*`, `zz_comp_probe_main.go.bak` (~330 MB reclaimed;
      keep one canonical `build/`).
- [ ] A4. Add `rick*.exe` / `*.test.exe` / `rick.before-*` to `.gitignore` so this never
      recurs.

### B. Disk hygiene (high impact, low effort)
- [ ] B1. Delete or move `snapshots/C_Users_einme_Downloads` (64 GB — a shadow-repo
      snapshot of Downloads). Add a snapshot retention policy (keep N per project, prune
      > 30 days) in `internal/maintenance` if one exists, else a scheduled prune.
- [ ] B2. Archive sessions older than 60 days (35 MB today; `sessions` cmd already exists —
      add `--prune-older-than`).
- [ ] B3. Verify snapshots never include user dirs like Downloads by default (check
      snapshot root selection; RICK.md hard rule 3 says they stay outside the work tree,
      but the root picked was Downloads).

### C. CPU / allocation (S1, S2, S5 from CPU plan §3)
- [ ] C1. S1: memoize per-message token counts in the agent (key = message identity/hash)
      so steady turns skip re-tokenizing the stable prefix (`buildRequest`,
      `countMessages`/`countJSONValues`, `history.Retain`/`retainStable`).
- [ ] C2. S2: compute `byteLen` from the already-marshalled hash payload in
      `ChooseBoundaries`; drop the second `json.Marshal` per message.
- [ ] C3. S5: `/compact` and distill run `ChooseBoundaries` over the full history once —
      reuse the last pass's boundaries when the head hasn't changed since.
- [ ] C4. Convert the rickui selftest "STREAM CHUNK IS UNDER 2ms" into a `go test`
      (transcript_trim_test.go exists untracked — promote it; it must run in CI).
- [ ] C5. Re-run `G:\cpuusageopti.md` audit items against current HEAD; profile one long
      session (`pprof` on `renderMsg` + `buildRequest`) to confirm C1/C2 land.

### D. Cache-hit continuation (92% → 98% where the provider allows)
- [ ] D1. Per-vendor TTL-aware resume (reasonix gap, currently ❌): replace the hard-coded
      idle-gap heuristic with per-vendor `DefaultCacheTTL` (DeepSeek 24 h, DashScope 5 m,
      Anthropic 5 m / 1 h with long retention) for cold-resume prune decisions.
- [ ] D2. Extend `warm_parity_test.go` to assert the openai retention/key fields match
      between Warm and Stream for every retention mode (bug §3.2).
- [ ] D3. Warm on *detected* eviction uses the full view (P1c); measure whether a
      second-tier "warm the tail only" saves tokens when the eviction is partial.
- [ ] D4. Keep the LRU-window tuning (distill at 55%, LiveZoneTurns 1) and validate on a
      paid non-free tier where provider evictions are rarer; publish the paid-tier
      benchmark as the 98% target.

### E. Tooling & verification
- [ ] E1. Land `cache-impact` workflow + `check-cache-guard.sh` (already written, untracked).
- [ ] E2. `analyze_sessions.py`: add per-session `reason:unexpected` counter to the summary
      output so a regression trips the analyzer, not just the harness.
- [ ] E3. Add `go test` coverage for `Warm` on `CacheRetentionNone` (one-off calls must not
      ride a session's cache — matches Stream's behavior).
- [ ] E4. Consider a `rick doctor` check for snapshot size + stale exe detection.

## 5. Success metrics

- A: `git status` clean after one reviewed commit; CI (build, vet, tests, cache-impact
  guard) green on it.
- B: `~/AppData/Local/rick/snapshots` ≤ 2 GB; repo root reclaims ~330 MB.
- C: per-chunk render cost flat 500 vs 1000 messages (within 2×); per-turn tokenization
  passes drop from 3+ to ≤1 on a steady prefix; rickui selftest guard passes in CI.
- D: 0 client divergences stays; paid-tier benchmark ≥ 96% TOTAL; warm/stream parity test
  covers all retention modes.
- No regressions: `go build ./...`, `go test ./...`, `go vet ./...`, `gofmt` clean.


## 6. Execution status (same day)

| Item | Status | Evidence |
|---|---|---|
| A1 WIP committed | ✅ | `ad5bce5` feat(cache): warm/stream byte parity, real-runner harness, byte-stable boot |
| A2 cache-impact labels in commit | ✅ | Headers on the commit message; gate script verified (7 scenarios: pass/fail/review-required/non-sensitive) |
| A3 stale binaries deleted | ✅ | Root: 328 MB of exes/backups removed; project 853 MB → 441 MB |
| A4 gitignore | ✅ | `rick.before-*`, `zz_*.go.bak`, `*.test.exe`, `tmp/` |
| B1 snapshot retention | ✅ | `internal/maintenance/prune.go` (`PruneSnapshots` + stat-only `PruneSnapshotDirs`), `rick maintenance prune-snapshots`, background prune at startup; tests |
| B2 sessions prune | ✅ | `Store.PruneOlderThan` + `current.json` cleanup, `rick sessions prune --older-than`; tests |
| B3 user-folder snapshot guard | ✅ | `isWellKnownUserDir` refuses Downloads/Documents/Desktop/Pictures/Videos/Music + profile root; tests |
| C1 token-count memo | ✅ | `tokens.Count` bounded memo (32 MiB/4096 FIFO); `TestCountMemoReturnsIdenticalResultsAndStaysBounded` |
| C2 single marshal in boundaries | ✅ | `messageHashBytes`; no second `json.Marshal` per message |
| C3 boundary reuse | ✅ | Per-message arrays + prefix sums/hashes cached; head change invalidates (tested); 2.0× measured |
| C4 trim regression in CI | ✅ | `transcript_trim_test.go` in-tree; `ci.yml` runs `go test ./...` |
| C5 audit re-run | ✅ | F-01/02/03/04/08 already fixed at HEAD (verified); F-05/06/07/09/10 deferred (below); boundary pass benchmarked 1.61 ms → 0.80 ms |
| D1 per-vendor TTL | ✅ | `provider.DefaultCacheTTL` (deepseek-line 24h, anthropic 5m/1h-long, default 5m); wired into agent + TUI; tests |
| D2 warm parity fix | ✅ | Warm no longer overrides retention/key; `TestWarmAndStreamCacheFieldsMatchEveryRetention` covers auto/long/none × openai/deepseek |
| D3 tail-only warm measurement | ⏸ deferred | Needs paid tier; P1c full-view re-warm already in place; record eviction-kind stats first |
| D4 paid-tier benchmark | ⏸ deferred | Needs paid API key; free flash tier evictions cap the achievable rate |
| E1 cache-impact workflow | ✅ | `.github/workflows/cache-impact.yml` + `scripts/check-cache-guard.sh` committed in A1, script tested |
| E2 analyzer unexpected counter | ✅ | `scripts/analyze_sessions.py` (moved from tmp/) prints `unexp` column + summary warning |
| E3 Warm CacheRetentionNone test | ✅ | Covered by the parity matrix (openai/None, deepseek/None) |
| E4 doctor checks | ✅ | `checkSnapshots` (stale trees) + `checkStaleBinaries` (`rick.before-*`, `*.test.exe`) |

Deferred (documented, not forgotten): F-05 plugin slice cache, F-06 session index,
F-07 incremental settled join, F-09 artCache eviction, F-10 usage ring buffer,
D3/D4 paid-tier validation. All were scoped out as architectural or
environment-blocked; each is a small standalone follow-up.

Final state: `go build ./...`, `go vet ./...`, `go test ./...` (33 packages) green;
deployed `rick.exe` rebuilt from this tree and byte-verified.

## 7. Execution status — round 2 (deferred items + cache-miss investigation)

| Item | Status | Evidence |
|---|---|---|
| F-05 plugin slice cache | ✅ already fixed at HEAD | `Registry.refreshActiveLocked` keeps an immutable `r.active` snapshot; `activePlugins()` returns it (plugin.go:239) |
| F-07 incremental settled join | ✅ already fixed at HEAD | transcript.go:102 — settled prefix cached, re-joined only when a block is dirty; the audit's O(n)-per-frame join was the trimTranscript bug's symptom |
| F-09 artCache eviction | ✅ | Bounded at 64 widths with FIFO eviction (art.go `cacheArt`) |
| F-10 usage tracker memory | ✅ resolved by inspection | Tracker holds per-(date,model) aggregates only; session `Requests` rows are ~100 B each and persisted |
| F-06 session search index | ⏸ deferred | Would add a second write path to the hot Save; current cost ~200 ms on a user-invoked CLI (415 sessions) |
| D3 eviction labels | ✅ | `RequestUsage.Eviction` persisted per request; `observeCacheUsage` returns the cause; analyzer prints it |
| D4 paid-tier harness | ✅ flag exists | `cachehit --idle <dur>` measures the endpoint's real gap TTL (needs a paid key to run) |
| Cache-miss investigation | ✅ | `docs/cache-miss-analysis-2026-08-08.md`: the session's single 51k re-bill is a provider-side eviction at the user-turn boundary (0 divergences, gap-free benchmark evicts identically, immediate re-cache) |
